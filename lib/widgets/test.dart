import 'package:flutter/cupertino.dart';

class TestWide extends StatelessWidget {
  const TestWide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
